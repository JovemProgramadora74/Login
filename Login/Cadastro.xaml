<Window x:Class="Login.Cadastro"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Cadastro" Height="450" Width="800">
    <Grid Background="#264ECA">
        <StackPanel VerticalAlignment="Center" Margin="20">
            <TextBlock
                Text="Cadastro de Usuário"
                FontSize="32"
                TextAlignment="Center"
                Margin="0 0 0 20"
                Foreground="White" />

            <!-- TextBlock/ TextBox / Button -->
            <TextBlock Text="Nome de usuário" FontSize="20" Width="300" Foreground="White" />
            <Border CornerRadius="10" Width="300" Height="45" Background="White" Margin="0,5,0,15">
                <TextBox FontSize="16" x:Name="tbUsuario" Background="Transparent" BorderThickness="0"
                         VerticalContentAlignment="Center" />
            </Border>

            <TextBlock Text="Senha do usuário" FontSize="20" Width="300" Foreground="White" />
            <Border CornerRadius="10" Width="300" Height="45" Background="White" Margin="0,5,0,15">
                <TextBox FontSize="16" x:Name="tbSenha" Background="Transparent" BorderThickness="0"
                         VerticalContentAlignment="Center" />
            </Border>

            <Button Content="Cadastrar" Background="White" Foreground="#2148C0" Width="300" Height="45" Margin="8"
                    Click="BotaoCadastrar" />
        </StackPanel>
    </Grid>
</Window>